# AI-Powered Cybersecurity Control Center (ACCC)

> Full README written in Phase 9. See Architecture Plan v2.0 for system documentation.

## Quick Start

```bash
cp .env.example .env
# Fill in OPENAI_API_KEY, POSTGRES_PASSWORD, JWT_SECRET in .env
docker compose up --build
```

Access the application at **http://localhost:3000**
